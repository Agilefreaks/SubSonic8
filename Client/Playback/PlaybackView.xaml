<Page
    x:Class="Subsonic8.Playback.PlaybackView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:cal="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="PlaylistItem">
            <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Stretch" Width="Auto" Height="Auto" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image Source="{Binding Image}"/>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Artist}"></TextBlock>
                    <TextBlock Text="{Binding Title}"></TextBlock>
                </StackPanel>

                <TextBlock Grid.Column="2" Text="{Binding Duration}"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="GoBack" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock Text="SubSonic8" Style="{StaticResource PageHeaderTextStyle}" Grid.Column="1" />
        </Grid>

        <ListView Grid.Row="1" 
                    ItemsSource = "{Binding PlaylistItems}"  ItemTemplate = "{StaticResource PlaylistItem}" 
                    SelectionMode = "Multiple"
                    CanDragItems = "True" CanReorderItems = "True" AllowDrop = "True"
                    IsItemClickEnabled = "True"
                    cal:Message.Attach = "[Event ItemClick] = [Action StartPlayback($sender, $eventArgs)]"/>
    </Grid>
</Page>