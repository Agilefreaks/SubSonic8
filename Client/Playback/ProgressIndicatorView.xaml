<UserControl
    x:Class="Subsonic8.Playback.ProgressIndicatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Subsonic8.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <converters:IntToTimeStringConverter x:Name="IntToTimeStringConverter" />
    </UserControl.Resources>
    <Slider Maximum="{ Binding ItemDurationInSeconds}" StepFrequency="{Binding ProgressStepFrequency}" Value="{ Binding PlaybackProgressInSeconds}" 
            Style="{StaticResource ProgressBarStyle}" ThumbToolTipValueConverter="{StaticResource IntToTimeStringConverter }" IsEnabled="True">

    </Slider>
</UserControl>
